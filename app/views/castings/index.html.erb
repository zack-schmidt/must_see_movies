<div class="page-header">
  <h1>
    Castings
    <a href="/castings/new" class="btn btn-lg btn-success">New Casting</a>
  </h1>
</div><div class="row mb-1">
  <div class="col-md-12">
    <%= search_form_for @q, :class => collapse_search_form, :id => "castings_filters" do |f| %>
      <p class="lead">Narrow results:</p>

        <div class="form-group">
          <%= f.label :created_at, "Created at" %>
          <div class="row">
            <div class='col-md-6 mb-1'>
              <div class='input-group date' id='created_atgteqPicker'>
                  <%= f.text_field :created_at_gteq, :class => "form-control", :placeholder => "Created at greater than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>

            <div class='col-md-6'>
              <div class='input-group date' id='created_atlteqPicker'>
                  <%= f.text_field :created_at_lteq, :class => "form-control", :placeholder => "Created at less than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>
          </div>
      </div>
      <script type="text/javascript">
          $(function () {
              $('#created_atgteqPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atlteqPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atgteqPicker').on("dp.change", function (e) {
                  $('#created_atlteqPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#created_atlteqPicker').on("dp.change", function (e) {
                  $('#created_atgteqPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

        <div class="form-group">
      <%= f.label :movie_title_cont, "Movie title contains" %>
      <%= f.text_field :movie_title_cont, :class => "form-control", :placeholder => "Movie title contains" %>
    </div>

<div class="form-group">
          <%= f.label :created_at, "Actor Created at" %>
          <div class="row">
            <div class='col-md-6 mb-1'>
              <div class='input-group date' id='actorgteqcreated_atPicker'>
                  <%= f.text_field :actor_created_at_gteq, :class => "form-control", :placeholder => "Actor Created at greater than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>

            <div class='col-md-6'>
              <div class='input-group date' id='actorlteqcreated_atPicker'>
                  <%= f.text_field :actor_created_at_lteq, :class => "form-control", :placeholder => "Actor Created at less than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>
          </div>
      </div>
      <script type="text/javascript">
          $(function () {
              $('#actorgteqcreated_atPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#actorlteqcreated_atPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#actorgteqcreated_atPicker').on("dp.change", function (e) {
                  $('#actorlteqcreated_atPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#actorlteqcreated_atPicker').on("dp.change", function (e) {
                  $('#actorgteqcreated_atPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

      <%= f.submit :class => "btn btn-primary btn-block mb-1" %>

      <a href="/castings" class="btn btn-default btn-block mb-1">Clear filters</a>
    <% end %>
    <a class="btn btn-default btn-block mb-1" data-toggle="collapse" href="#castings_filters">
      Show Filters
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Movie</th>
        <th>Actor</th>
        <th>Actions</th>
      </tr>

      <% @castings.each do |casting| %>
      <tr>
        <td>
          <% if casting.movie.present? %>
            <a href="/movies/<%= casting.movie.id %>">
              <%= casting.movie.title %>
            </a>
          <% end %>
        </td>
        <td>
          <% if casting.actor.present? %>
            <a href="/actors/<%= casting.actor.id %>">
              <%= casting.actor.created_at %>
            </a>
          <% end %>
        </td>
        <td>
          <a href="/castings/<%= casting.id %>" class="btn btn-primary">Show</a>
          <a href="/castings/<%= casting.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_casting/<%= casting.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @castings, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
